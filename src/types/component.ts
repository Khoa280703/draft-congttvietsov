export interface StatisticCardData {
  period: string;
  plan: number;
  actual: number;
}

export interface TrainingItemData {
  id: number | string;
  title: string;
  duration: string;
  startDate: string;
}
